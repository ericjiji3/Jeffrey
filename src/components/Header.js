import React from "react"
import styles from "./Header.css";

function Header(){
  return(
    <div>
      <h1 className = "head text-center mx-auto mt-4">JEFFREY</h1>
    </div>
  )
}

export default Header;
